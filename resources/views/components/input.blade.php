<div class="relative mb-4 space-y-2">
    <label for="{{ $name }}" class="text-sm font-medium text-gray-700">{{ ucfirst($name) }}</label>
    <input type="{{ $type }}" id="{{ $name }}" name="{{ $name }}" class="w-full rounded border border-gray-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none text-gray-700 py-1 px-3">
</div>